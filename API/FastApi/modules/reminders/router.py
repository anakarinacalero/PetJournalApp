from fastapi import APIRouter

router = APIRouter()

# TODO(reminders): implementar endpoints de recordatorios siguiendo
# FastApi/modules/users/router.py como referencia (router delgado -> Depends(service)).
# El dispatcher periodico (APScheduler) va aparte, ver PetCare-Journal-SPEC.md seccion 3.2.
