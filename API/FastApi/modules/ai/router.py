from fastapi import APIRouter

router = APIRouter()

# TODO(ai): implementar symptom-check y snack-recipe siguiendo
# FastApi/modules/users/router.py como referencia (router delgado -> Depends(service)).
# Ver PetCare-Journal-SPEC.md secciones 3.3 y 3.4 para el flujo de guardarrailes obligatorio.
